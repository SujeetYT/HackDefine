{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-dependencies" : "cd ../frontend/save-food-app && npm install",
    "build" : "cd ../frontend/save-food-app && npm run build",
    "heroku-postbuild" : "npm run install-dependencies && npm run build",
    "start": "node index.js"
  },
  "author": "Sujeet Kumar",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "mongoose": "^6.3.2",
    "nodemon": "^2.0.16"
  }
}
